// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("libipv6", "for libipv6 support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("libipv6", "enable libipv6 support", "no");

if (PHP_LIBIPV6 != "no") {
	EXTENSION("libipv6", "libipv6.c");
}

